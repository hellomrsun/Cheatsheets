# Angular Cheatsheet

#### Check Angular CLI version

```cmd
> ng --version 
```
or 
```cmd
> ng v
```


#### Create module with routing

```cmd
> ng g module {moduleNameWithFullPath} --routing=true
```
